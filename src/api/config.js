/**
 * API Configuration
 *
 * Centralized configuration for all API-related settings.
 * Update BASE_URL here when switching between environments.
 */

export const API_CONFIG = {
  BASE_URL: "https://meditation-teachers.onrender.com",
  TIMEOUT: 15000, // 15 seconds
};
